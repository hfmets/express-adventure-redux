<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css2?family=Source+Code+Pro&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/main.css">
</head>

<body>
    <h3>WELCOME TO THE JUNGLE</h3>
    <br>
    <p><%= forward2.script %> </p>
    <br>
    <% if (item == "cards") { %>
    <% for( let index = 0; index < forward2.choices.cards.length; index++ ) { %>
    <a href="/<%= forward2.choices.cards[index].path %> "><%= forward2.choices.cards[index].choice %> </a><br>
    <% } %>
    <% } else if (item == "water") { %>
    <% for( let index = 0; index < forward2.choices.water.length; index++ ) { %>
    <a href="/<%= forward2.choices.water[index].path %> "><%= forward2.choices.water[index].choice %> </a><br>
    <% } %>
    <% } else if (item == "machete") { %>
    <% for( let index = 0; index < forward2.choices.machete.length; index++ ) { %>
    <a href="/<%= forward2.choices.machete[index].path %> "><%= forward2.choices.machete[index].choice %> </a><br>
    <% } %>
    <% } %>

</body>

</html>